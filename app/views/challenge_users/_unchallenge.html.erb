<%= form_with(model: @challenge_user, html: { method: :delete }, local: true) do |f| %>
    <div><%= hidden_field_tag :challenge_id, challenge_id %></div>
    <div><%= hidden_field_tag :user_id, user_id %></div>
    <%= f.submit "Challenged", class: "btn btn-info" %>
<% end %>